<Styles xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             xmlns:ui="clr-namespace:FluentAvalonia.UI.Controls;assembly=FluentAvalonia"
             xmlns:media="clr-namespace:FluentAvalonia.UI.Media;assembly=FluentAvalonia">

    <!-- Note about Sytling in FluentAvalonia
    MS creates a set of base colors/brushes (see ThemeStylesLight/ThemeStylesDark) and resuses
    them everywhere. To be specific, they use StaticResource with a refernce to the base
    brushes to use, eg:
    <StaticResource x:Key="ButtonBackground" ResourceKey="SystemControlBackgroundBaseLowBrush" />
    Avalonia doesn't support this, so instead we map directly to the brush we want to use, however,
    the mappings as they're laid out in WinUI themeresources.xaml will be specified here
    
    <SolidColorBrush x:Key="ButtonBackground" Color="{DynamicResource SystemControlBackgroundBaseLowBrush}"/>
    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{DynamicResource SystemBaseHighBrush}" Opacity="0.1"/>
    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"/>
    <SolidColorBrush x:Key="ButtonBackgroundDisabled" Color="{DynamicResource SystemControlBackgroundBaseLowBrush}" />
    <SolidColorBrush x:Key="ButtonForeground" Color="{DynamicResource SystemControlForegroundBaseHighBrush}" />
    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{DynamicResource SystemControlHighlightBaseHighBrush}" />
    <SolidColorBrush x:Key="ButtonForegroundPressed" Color="{DynamicResource SystemControlHighlightBaseHighBrush}" />
    <SolidColorBrush x:Key="ButtonForegroundDisabled" Color="{DynamicResource SystemControlDisabledBaseMediumLowBrush}" />
    <SolidColorBrush x:Key="ButtonBorderBrush" Color="{DynamicResource SystemControlForegroundTransparentBrush}" />
    <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="{DynamicResource SystemControlForegroundTransparentBrush}" />
    <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="{DynamicResource SystemControlHighlightTransparentBrush}" />
    <SolidColorBrush x:Key="ButtonBorderBrushDisabled" Color="{DynamicResource SystemControlDisabledTransparentBrush}" />

    <SolidColorBrush x:Key="AccentButtonBackground" Color="{DynamicResource SystemControlBackgroundAccentBrush}" />
    <SolidColorBrush x:Key="AccentButtonBackgroundPointerOver" Color="{DynamicResource SystemAccentBrushLight1}" />
    <SolidColorBrush x:Key="AccentButtonBackgroundPressed" Color="{DynamicResource SystemAccentBrushDark1}" />
    <SolidColorBrush x:Key="AccentButtonBackgroundDisabled" Color="{DynamicResource SystemControlBackgroundBaseLowBrush}" />
    <SolidColorBrush x:Key="AccentButtonForeground" Color="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
    <SolidColorBrush x:Key="AccentButtonForegroundPointerOver" Color="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
    <SolidColorBrush x:Key="AccentButtonForegroundPressed" Color="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
    <SolidColorBrush x:Key="AccentButtonForegroundDisabled" Color="{DynamicResource SystemControlDisabledBaseMediumLowBrush}" />
    <SolidColorBrush x:Key="AccentButtonBorderBrush" Color="{DynamicResource SystemControlForegroundTransparentBrush}" />
    <SolidColorBrush x:Key="AccentButtonBorderBrushPointerOver" Color="{DynamicResource SystemControlForegroundTransparentBrush}" />
    <SolidColorBrush x:Key="AccentButtonBorderBrushPressed" Color="{DynamicResource SystemControlHighlightTransparentBrush}" />
    <SolidColorBrush x:Key="AccentButtonBorderBrushDisabled" Color="{DynamicResource SystemControlDisabledTransparentBrush}" />

    -->

    <Styles.Resources>
        <Thickness x:Key="ButtonPadding">8,5,8,6</Thickness>
        <Thickness x:Key="ButtonBorderThemeThickness">1</Thickness>
    </Styles.Resources>

    <!--Default Button Style-->
    <Style Selector="Button">
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseLowBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundTransparentBrush}"/>
        <Setter Property="BorderThickness" Value="{DynamicResource ButtonBorderThickness}" />
        <Setter Property="Padding" Value="{StaticResource ButtonPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}"/>
        <Setter Property="Template">
            <ControlTemplate>
                <ContentPresenter Name="ContentPresenter"
                                  Background="{TemplateBinding Background}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  Content="{TemplateBinding Content}"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  CornerRadius="{StaticResource ControlCornerRadius}"
                                  Padding="{TemplateBinding Padding}"
                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style Selector="Button:pointerover /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource SystemBaseHighColor}" Opacity="0.1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlHighlightBaseHighBrush}"/>
    </Style>
    <Style Selector="Button:pressed /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlHighlightBaseHighBrush}"/>
    </Style>
    <Style Selector="Button:disabled /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseLowBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlDisabledTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlDisabledBaseMediumLowBrush}"/>
    </Style>

    <!--Accent Button Style-->
    <Style Selector="Button.Accent">
        <Setter Property="Foreground" Value="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundTransparentBrush}"/>
        <Setter Property="Template">
            <ControlTemplate>
                <ContentPresenter Name="ContentPresenter"
                                  Background="{TemplateBinding Background}"
                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Content="{TemplateBinding Content}"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  CornerRadius="{StaticResource ControlCornerRadius}"
                                  Padding="{TemplateBinding Padding}"
                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </ControlTemplate>
        </Setter>
    </Style>
    <Style Selector="Button.Accent:pointerover /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemAccentBrushLight1}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlBackgroundChromeWhiteBrush}"/>
    </Style>
    <Style Selector="Button.Accent:pressed /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemAccentBrushDark1}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlBackgroundChromeWhiteBrush}"/>
    </Style>
    <Style Selector="Button.Accent:disabled /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseLowBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlDisabledTransparentBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlDisabledBaseMediumLowBrush}"/>
    </Style>

    <!--Reveal Button Style {DynamicResource SystemControlHighlightBaseMediumLowBrush}-->
    <Style Selector="Button.Reveal">
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{DynamicResource ButtonBorderThickness}" />
        <Setter Property="Template">
            <ControlTemplate>
                <Panel>
                    <ContentPresenter Name="ContentPresenter"
                                 Background="{TemplateBinding Background}"
                                 TextBlock.Foreground="{TemplateBinding Foreground}"
                                 BorderBrush="{TemplateBinding BorderBrush}"
                                 BorderThickness="{TemplateBinding BorderThickness}"
                                 Content="{TemplateBinding Content}"
                                 ContentTemplate="{TemplateBinding ContentTemplate}"
                                 CornerRadius="{StaticResource ControlCornerRadius}"
                                 Padding="{TemplateBinding Padding}"
                                 HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                 VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    <media:RevealBorder RevealGradientStops="{DynamicResource RevealBorderGradientStops}"
                                        RevealRadius="{DynamicResource RevealBrushSize}"
                                        CornerRadius="{StaticResource ControlCornerRadius}"
                                        BorderThickness="{StaticResource RevealBorderThickness}"
                                         /><!--PointerOverBorderBrush="{TemplateBinding BorderBrush}"-->
                </Panel>
            </ControlTemplate>
        </Setter>
    </Style>
    <Style Selector="Button.Reveal:pointerover /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource SystemBaseHighColor}" Opacity="0.1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightBaseMediumLowBrush}"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlHighlightBaseHighBrush}"/>
    </Style>
    <Style Selector="Button.Reveal:pressed /template/ ContentPresenter#ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource SystemControlHighlightBaseHighBrush}"/>
    </Style>
    
    
</Styles>